logging.level.com.example.chatapp=DEBUG
#spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Use embedded Kafka broker for tests
spring.kafka.bootstrap-servers=${spring.embedded.kafka.brokers}
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.group-id=chat-service

server.id=server-1

chat.server.address=localhost:8080

google.oauth.token-uri=http://localhost:54321/token
google.oauth.userinfo-uri=http://localhost:54321/userinfo
google.oauth.client-id=test-client-id
google.oauth.client-secret=test-client-secret
google.oauth.redirect-uri=http://localhost:8080/auth/oauth/google/callback

aws.region=us-east-1
aws.s3.bucket-name=test-bucket
aws.s3.endpoint=http://localhost:4566
spring.main.allow-bean-definition-overriding=true

